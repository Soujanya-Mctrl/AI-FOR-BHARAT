export const verdictExecutorService = {
    executeVerdict: async (investigationId: string, verdict: any) => {
        console.log(`[VerdictExecutor] Stubbed executeVerdict for ${investigationId}`);
    }
};
